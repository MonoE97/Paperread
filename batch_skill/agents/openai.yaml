interface:
  display_name: "Paperread Batch"
  short_description: "Parallel paperread orchestration with verified Zotero writes"
  default_prompt: "Use $paperread-batch to orchestrate multiple $paperread paper analyses, write Zotero-backed notes by default, verify them, and produce a batch report."
policy:
  allow_implicit_invocation: true
