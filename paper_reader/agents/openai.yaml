interface:
  display_name: "paper_reader"
  short_description: "Paper Reader 2.1 immutable evidence-to-note workflow"
  default_prompt: "Use $paper_reader through the released Paper Reader 2.1 grouped CLI runtime. Route existing local PDF and directory paths before Zotero text and keep them local-only. For Zotero items, inspect the immutable Extra-link plan, capture eligible public pages read-only, ingest only plan-bound results, and use them solely for validated secondary cross-checks while PDF evidence remains canonical. Create immutable review and candidate artifacts, and let only the external agent write an explicitly authorized Zotero note through MCP before strong read-only verification. Reject V1 and unversioned inputs as historical-only; do not migrate or fall back."
policy:
  allow_implicit_invocation: true
